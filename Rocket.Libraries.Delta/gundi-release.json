{
    "Label": "Delta Self Deploy",    
    "BuildCommands":[
        "dotnet test",
        "dotnet build",
        "dotnet publish -r linux-x64 -c Release --self-contained true",
        "dotnet publish -r win-x64 -c Release --self-contained true"
    ],
    "DisabledStages":[
        "CopyToStagingDirectory",
        "PublishToRepository"
    ],
    "BuildOutputDirectory":"./Rocket.Libraries.Delta/out"
}